# Plan: Remove Create PR from ChatFooter popover

## Summary

Remove the "Create PR" button from ChatFooter's advanced options popover. A contextual "Create PR" / "Update PR" button already exists in `ChatView.tsx` — it appears as a `bottomAction` on the last `SessionTurn` when `hasFileChanges && isIdle`. That's the correct location.

## Changes

### File: `src/components/chat/ChatFooter.tsx`

1. Remove the `CREATE_PR_PROMPT` constant (line 21)
2. Remove the `handleCreatePrSubmit` callback (lines ~120-124)
3. Remove the `<Button>` for "Create PR" inside the advanced popover (lines 326-334)
4. Update the comment on the popover (line 242) to remove "create PR" mention

### Verification

1. `bun run check`
2. `bun run build`
3. Confirm "Create PR" / "Update PR" still appears contextually after the last turn in ChatView (that code is untouched)
